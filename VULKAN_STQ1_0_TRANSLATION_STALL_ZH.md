# Vulkan STQ1_0 翻译周期性卡顿问题记录

## 1. 问题现象

使用 Vulkan 版本加载 `Hy-MT2-1.8B-1.25Bit.gguf` 启动服务器后，通过
`D:\devspace\test_translate` 连续提交不同长度的翻译请求，会出现以下现象：

- 开始时可以正常翻译几条。
- 随后某一条请求会停顿数秒到二十多秒。
- 停顿结束后又能连续处理几条，然后再次长时间停顿。
- 同一模型在提交 `b93c17be52eca911405df76ed2d0f1edae626dd9` 的 CUDA
  版本上没有这种周期性卡顿。

这不是服务器死锁，也不是翻译请求积压。卡顿期间程序最终可以自行恢复，且已经
完成编译的输入形状再次出现时通常不会重复产生同等时长的停顿。

## 2. 根因

STQ1_0 的提示词处理曾让多列矩阵计算走 Vulkan 的 DMMV/MMVQ 路径。该路径会
根据列数生成不同的 shader specialization，例如
`mul_mat_vec_stq1_0_q8_1_f32` 的 `NUM_COLS=2..8`。

这些 Vulkan pipeline 由 NVIDIA 驱动在首次使用相应 specialization 时编译。
实测每个新 specialization 的首次编译约需要 4 到 13 秒。翻译文本长度不同，
会在不同请求中依次首次触发不同的列数，因此用户看到的不是一次性的启动卡顿，
而是以下过程：

```text
处理若干请求 -> 遇到新的列数 -> 驱动编译对应 pipeline -> 长时间停顿
             -> 处理若干请求 -> 遇到另一个新列数 -> 再次停顿
```

原版 60 条翻译中共记录到 6 次明显停顿，最长约 26.2 秒，总耗时约 83.6 秒。
停顿次数与运行过程中首次遇到的多列 specialization 相符。

## 3. 排查过程

排查时先对每条翻译请求单独计时，并对比 Vulkan 与 CUDA。CUDA 没有相同的
周期性长尾，说明问题集中在 Vulkan 后端，而不是模型、服务端请求调度或翻译
脚本。

随后确认长尾发生在 STQ1_0 多列 mat-vec pipeline 首次使用时。仅禁止多列
MMVQ、让计算继续走 float DMMV 后，60 条请求仍出现 6 次停顿，最长约 5.5 秒，
总耗时约 32.5 秒。停顿有所缩短但没有消失，说明问题不是 MMVQ shader 中的
codebook 查表本身，而是多列 mat-vec specialization 的首次编译成本。

曾尝试把 32 项 codebook 动态查表改为位运算。该方案只能部分改善编译时间，
没有消除多个 specialization 被按需编译的根因，因此没有作为卡顿修复保留。

## 4. 解决方法

核心修复位于 `ggml/src/ggml-vulkan/ggml-vulkan.cpp`：当左矩阵类型为
`GGML_TYPE_STQ1_0` 且输出列数大于 1 时，不再选择多列 mat-vec 路径，改用
已有的批量矩阵乘路径。

对应的核心提交为：

```text
d8ba3e29a1e42b3d0db8fb0084cbc8ebd40afcce
vulkan: use matmul for batched STQ1_0
```

关键条件如下：

```cpp
!(src0->type == GGML_TYPE_STQ1_0 && dst->ne[1] > 1)
```

后续提交 `38dea724df1c6c574697e7d5d5a628c3f9764ac3` 进一步优化了 STQ1_0
提示词处理：较大批量先完整反量化为 F16，再使用 Vulkan F16 矩阵乘，并修复了
反量化 shader 的越界索引。它提升了提示词吞吐，但消除周期性卡顿的关键仍是
不再为 STQ1_0 多列输入选择 mat-vec specialization。

## 5. 为什么保留单列 MMVQ

`dst->ne[1] == 1` 的单列计算仍走 MMVQ。这是逐 token 生成阶段的主要路径，
保留它可以避免降低生成速度。

修复只改变列数大于 1 的提示词批处理路径。这样既避免在真实翻译请求中陆续触发
多个昂贵的多列 specialization，又保留了单列 MMVQ 对生成阶段的性能优势。

## 6. 验证结果

同一批 60 条翻译的对比如下：

| 版本 | 明显停顿 | 最长单条耗时 | 总耗时 |
| --- | ---: | ---: | ---: |
| 原版多列 MMVQ/DMMV | 6 次 | 约 26.2 秒 | 约 83.6 秒 |
| 仅禁用多列 MMVQ，仍使用 float DMMV | 6 次 | 约 5.5 秒 | 约 32.5 秒 |
| 多列改用批量矩阵乘 | 仅首条冷启动 | 约 4.19 秒 | 约 16.3 秒 |

最终方案中，第 2 到 60 条请求的最大耗时约为 0.61 秒。继续连续测试 200 条：

- 超过 1 秒的请求为 0 条。
- 最大单条耗时约 0.607 秒。
- 总耗时约 40.6 秒。

首条约 4.19 秒属于进程启动后的必要冷编译，与修复前在运行过程中反复遇到新
specialization 所产生的周期性卡顿不同。

在 Vulkan1、NVIDIA GeForce RTX 4090 Laptop GPU 上，STQ1_0 `MUL_MAT`
正确性测试通过以下 3 个用例：

- `m=256, n=1, k=256`
- `m=256, n=4, k=256`
- `m=512, n=1, k=1024`

后续提示词处理优化还增加并通过了 `m=256, n=16, k=256` 用例。

## 7. 结论

翻译过程中的周期性长时间卡顿是 NVIDIA Vulkan 驱动首次编译多个 STQ1_0
多列 mat-vec specialization 导致的。由于不同长度的请求会在运行过程中逐步
触发不同列数，编译成本被分散到多条翻译之间，从而表现为间歇性卡顿。

解决方法是让 STQ1_0 的多列提示词计算使用已有批量矩阵乘路径，只为单列生成
保留 MMVQ。该修改消除了运行过程中的周期性长尾，同时保留了逐 token 生成路径
的性能。
